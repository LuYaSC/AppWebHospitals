import { PatientInformationResult } from './patient-information-result';

describe('PatientInformationResult', () => {
  it('should create an instance', () => {
    expect(new PatientInformationResult()).toBeTruthy();
  });
});
