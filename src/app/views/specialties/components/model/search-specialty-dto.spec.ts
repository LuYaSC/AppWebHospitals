import { SearchSpecialtyDto } from './search-specialty-dto';

describe('SearchSpecialtyDto', () => {
  it('should create an instance', () => {
    expect(new SearchSpecialtyDto()).toBeTruthy();
  });
});
